// This lets footers stick to the bottom
document.getElementsByTagName("body")[0].classList += " d-flex flex-column min-vh-100"; // Don't remove space before classes, this is to separate these classes from any existing classes specified in the HTML files
// This adds a bit of space before the footer
document.getElementsByTagName("main")[0].classList += " mb-5"; // Don't remove space before classes, this is to separate these classes from any existing classes specified in the HTML files